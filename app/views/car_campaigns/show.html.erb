<% set_meta_tags(title:       @car_campaign.title,
                 description: @car_campaign.description,
                 canonical:   @car_campaign.canonical_url,
                 alternate:   @car_campaign.alternate_options) %>

<%= render 'content', car_campaign: @car_campaign %>

