<%
   meta_tags = {
     canonical: car_campaign_url('revealed-the-true-cost-of-buying-a-car', locale: :en),
     alternate: {
       en: car_campaign_url('revealed-the-true-cost-of-buying-a-car', locale: :en),
       cy: car_campaign_url('edrychwch-cost-gwirioneddol-prynu-car', locale: :cy)
     }
   }.merge(t('.meta_tags'))

   set_meta_tags(meta_tags)
%>

<% content_for(:alternate_link) do %>
  <%= link_to(car_campaign_url('edrychwch-cost-gwirioneddol-prynu-car', locale: :cy), lang: :cy, class: 'footer-secondary__link') do %>
    <span class="icon icon--globe"></span>
    <%= t('locales.cy') %>
  <% end %>
<% end %>

<%= render 'content' %>
